{
  "name": "@doclyst/web",
  "version": "0.1.0",
  "description": "Local browser interface for Doclyst. Runs entirely on your device.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/bundle-single.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@doclyst/core": "0.1.0"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  }
}
